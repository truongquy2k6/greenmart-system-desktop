<Window x:Class="GreenMart.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="GreenMart - Hệ thống quản lý" Height="750" Width="1300" 
        WindowStartupLocation="CenterScreen" WindowState="Maximized"
        WindowStyle="None" ResizeMode="NoResize"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="14"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">
    <Window.Resources>
        <Style x:Key="SidebarBtn" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#B2DFDB"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="Margin" Value="10,2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="8" Padding="16,0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="35"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <materialDesign:PackIcon x:Name="icon" Kind="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}" VerticalAlignment="Center" Width="22" Height="22" Foreground="{TemplateBinding Foreground}"/>
                                <TextBlock Grid.Column="1" Text="{TemplateBinding Content}" VerticalAlignment="Center" FontSize="14" FontWeight="Medium"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#2E7D32"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#A5D6A7"/>
                                <Setter Property="Foreground" Value="#1B5E20"/>
                                <Setter TargetName="icon" Property="Foreground" Value="#1B5E20"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="GroupLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#81C784"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="25,15,0,8"/>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- Sidebar -->
        <Border Background="#1B5E20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Logo -->
                <StackPanel Margin="20,25,20,15" Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Storefront" Width="32" Height="32" Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="GREEN MART" FontSize="22" FontWeight="Black" Foreground="White"/>
                        <TextBlock Text="Hệ thống quản lý" FontSize="12" Foreground="#A5D6A7" Margin="0,2,0,0"/>
                    </StackPanel>
                </StackPanel>
                
                <!-- Nav -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="0,10,0,20">

                        <!-- BÁN HÀNG - chỉ Admin + Bán hàng -->
                        <TextBlock Text="BÁN HÀNG" Style="{StaticResource GroupLabel}" x:Name="grpBanHang"/>
                        <RadioButton x:Name="btnPOS" Content="Bán hàng (POS)" Style="{StaticResource SidebarBtn}" GroupName="nav" Click="Nav_Click" Tag="Cart" Uid="POS"/>
                        
                        <!-- TỔNG QUAN - Admin + Bán hàng -->
                        <TextBlock Text="TỔNG QUAN" Style="{StaticResource GroupLabel}" x:Name="grpTongQuan"/>
                        <RadioButton x:Name="btnDashboard" Content="Bảng điều khiển" Style="{StaticResource SidebarBtn}" GroupName="nav" IsChecked="True" Click="Nav_Click" Tag="ViewDashboard" Uid="Dashboard"/>
                        
                        <!-- QUẢN LÝ - Admin + Kho -->
                        <TextBlock Text="QUẢN LÝ" Style="{StaticResource GroupLabel}"/>
                        <RadioButton x:Name="btnSanPham" Content="Sản phẩm" Style="{StaticResource SidebarBtn}" GroupName="nav" Click="Nav_Click" Tag="PackageVariant" Uid="SanPham"/>
                        <RadioButton x:Name="btnLoaiSP" Content="Loại sản phẩm" Style="{StaticResource SidebarBtn}" GroupName="nav" Click="Nav_Click" Tag="Shape" Uid="LoaiSP"/>
                        <RadioButton x:Name="btnNCC" Content="Nhà cung cấp" Style="{StaticResource SidebarBtn}" GroupName="nav" Click="Nav_Click" Tag="Truck" Uid="NCC"/>
                        <RadioButton x:Name="btnKhoHang" Content="Kho hàng" Style="{StaticResource SidebarBtn}" GroupName="nav" Click="Nav_Click" Tag="Warehouse" Uid="Kho"/>
                        <RadioButton x:Name="btnChiTietKho" Content="Chi tiết kho" Style="{StaticResource SidebarBtn}" GroupName="nav" Click="Nav_Click" Tag="WarehouseInsert" Uid="ChiTietKho"/>

                        <!-- KHÁCH HÀNG + HÓA ĐƠN - Admin + Bán hàng -->
                        <RadioButton x:Name="btnKH" Content="Khách hàng" Style="{StaticResource SidebarBtn}" GroupName="nav" Click="Nav_Click" Tag="AccountGroup" Uid="KH"/>
                        <RadioButton x:Name="btnHD" Content="Hóa đơn" Style="{StaticResource SidebarBtn}" GroupName="nav" Click="Nav_Click" Tag="Receipt" Uid="HD"/>

                        <!-- CHỈ ADMIN -->
                        <TextBlock x:Name="grpAdminOnly" Text="HỆ THỐNG" Style="{StaticResource GroupLabel}"/>
                        <RadioButton x:Name="btnNV" Content="Nhân viên" Style="{StaticResource SidebarBtn}" GroupName="nav" Click="Nav_Click" Tag="AccountTie" Uid="NV"/>
                        <RadioButton x:Name="btnCH" Content="Cửa hàng" Style="{StaticResource SidebarBtn}" GroupName="nav" Click="Nav_Click" Tag="Store" Uid="CH"/>
                        <RadioButton x:Name="btnKM" Content="Khuyến mãi" Style="{StaticResource SidebarBtn}" GroupName="nav" Click="Nav_Click" Tag="TagText" Uid="KM"/>
                        <RadioButton x:Name="btnCauHinh" Content="Cấu hình" Style="{StaticResource SidebarBtn}" GroupName="nav" Click="Nav_Click" Tag="Cog" Uid="CauHinh"/>
                        
                        <!-- LỊCH SỬ - chỉ Admin -->
                        <TextBlock x:Name="grpLichSu" Text="LỊCH SỬ" Style="{StaticResource GroupLabel}"/>
                        <RadioButton x:Name="btnLSTC" Content="Lịch sử truy cập" Style="{StaticResource SidebarBtn}" GroupName="nav" Click="Nav_Click" Tag="History" Uid="LSTC"/>
                        <RadioButton x:Name="btnLSCS" Content="Lịch sử chỉnh sửa" Style="{StaticResource SidebarBtn}" GroupName="nav" Click="Nav_Click" Tag="FileEdit" Uid="LSCS"/>
                    </StackPanel>
                </ScrollViewer>
                
                <!-- User Profile -->
                <Border Grid.Row="2" Padding="20" Background="#144C18">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Border Width="40" Height="40" CornerRadius="20" Background="#A5D6A7">
                            <TextBlock x:Name="txtUserInitial" Text="A" FontSize="20" FontWeight="Bold" Foreground="#1B5E20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center">
                            <TextBlock x:Name="txtUserName" Text="Nguyễn Văn An" FontWeight="SemiBold" Foreground="White" FontSize="14"/>
                            <TextBlock x:Name="txtUserRole" Text="Quản lý · CH001" Foreground="#A5D6A7" FontSize="12"/>
                        </StackPanel>
                        <Button Grid.Column="2" Style="{StaticResource MaterialDesignIconButton}" ToolTip="Đăng xuất" Foreground="#FFEBEE" Click="btnLogout_Click">
                            <materialDesign:PackIcon Kind="Logout" Width="20" Height="20"/>
                        </Button>
                    </Grid>
                </Border>
            </Grid>
        </Border>
        
        <!-- Content -->
        <Grid Grid.Column="1" Background="#F4F6F8">
            <Grid.RowDefinitions>
                <RowDefinition Height="65"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Header -->
            <Border Background="White" Padding="25,0">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="1" Opacity="0.05"/>
                </Border.Effect>
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <materialDesign:PackIcon Kind="Home" Width="20" Height="20" Foreground="#9E9E9E" VerticalAlignment="Center"/>
                        <materialDesign:PackIcon Kind="ChevronRight" Width="20" Height="20" Foreground="#9E9E9E" VerticalAlignment="Center" Margin="4,0"/>
                        <TextBlock x:Name="txtPageTitle" Text="Bảng điều khiển" FontWeight="Bold" FontSize="18" Foreground="#2C3E50" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <!-- Role Badge -->
                        <Border x:Name="roleBadge" CornerRadius="12" Padding="10,4" Margin="0,0,15,0" Background="#E8F5E9">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon x:Name="roleIcon" Kind="ShieldAccount" Width="16" Height="16" Foreground="#2E7D32" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                <TextBlock x:Name="txtRoleBadge" Text="Admin" Foreground="#2E7D32" FontSize="12" FontWeight="Bold" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <materialDesign:PackIcon Kind="CalendarBlank" Width="18" Height="18" Foreground="#78909C" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBlock x:Name="txtDate" Text="" Foreground="#78909C" FontSize="14" FontWeight="Medium" VerticalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Border>
            
            <!-- Page Content -->
            <Grid Grid.Row="1" Margin="25">
                <ContentControl x:Name="pageContent"/>
            </Grid>
        </Grid>
    </Grid>
</Window>
